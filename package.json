{
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.0.1",
    "dotenv": "^17.4.2"
  },
  "scripts": {
    "build": "cd client && bun install --production=false && bun run build && cd ../server && bun install --production=false && bunx prisma generate",
    "start": "cd server && bunx prisma db push && bun run index.ts",
    "test:e2e": "bun playwright test",
    "test:e2e:ui": "bun playwright test --ui",
    "test:e2e:headed": "bun playwright test --headed",
    "test": "bun playwright test"
  }
}
